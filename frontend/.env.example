# Reown AppKit project ID (falls back to the canonical shared ID)
NEXT_PUBLIC_REOWN_PROJECT_ID=
# Alchemy key for mainnet reads (falls back to publicnode)
NEXT_PUBLIC_ALCHEMY_API_KEY=
# Contract addresses — leave empty until deployed; UI shows pre-deploy state
NEXT_PUBLIC_DONATIONS_ADDRESS=
NEXT_PUBLIC_DONATIONS_DEPLOY_BLOCK=
NEXT_PUBLIC_REGISTRAR_ADDRESS=
NEXT_PUBLIC_L2_REGISTRY_ADDRESS=
NEXT_PUBLIC_USDC_ADDRESS=
# Gateway route that pins uploaded avatar images
NEXT_PUBLIC_AVATAR_UPLOAD_URL=
# Pinata (pin.sh)
PINATA_JWT=
# Gateway route the partner form posts to (/partner)
NEXT_PUBLIC_PARTNER_URL=
