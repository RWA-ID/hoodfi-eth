SIGNER_PRIVATE_KEY=
# Optional overrides
ROBINHOOD_RPC_URL=https://rpc.mainnet.chain.robinhood.com
L2_REGISTRY_ADDRESS=
# Avatar image uploads (/avatar/:label). Without it that route 503s.
PINATA_JWT=
# Partner enquiries (/partner). Without it that route 503s and nothing is dropped
# silently. Set in production with `wrangler secret put RESEND_API_KEY`.
RESEND_API_KEY=
